Covers:

Why Singleton for IdGenerator (thread-safe double-checked locking)

Why Strategy Pattern for billing (Open/Closed Principle)

Deep vs shallow copy decision (mutable nested objects)

Generic DataStore for type safety

Enum usage over string constants

Concurrency with ReadWriteLock in DataStore
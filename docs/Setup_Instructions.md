Includes:

JDK installation (version 17+ recommended)

IDE setup (IntelliJ/Eclipse/VSCode)

Environment variables (JAVA_HOME, PATH)

Compilation commands: javac -d out src/**/*.java

Execution: java -cp out com.airtribe.meditrack.Main

Command-line argument: --loadData for persistence